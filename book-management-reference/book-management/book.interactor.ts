import { IInteractor } from "../core/interactor";
const menu = `
    1.Add Book
    2.Edit Book
    3.Search Book
    4.<Previous Menu>
    `;
export class BookInteractor implements IInteractor {
  showMenu(): void {
    // const op=await
  }
}
